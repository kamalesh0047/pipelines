trigger:
  - main   # or your actual branch name

pool:
  name: 'report S.K'  # 👈 your self-hosted agent pool name

steps:
  - task: UseDotNet@2
    inputs:
      packageType: 'sdk'
      version: '6.x.x'  # adjust version if needed

  - script: dotnet build
    displayName: 'Build the project'

  - script: dotnet test
    displayName: 'Run tests'
